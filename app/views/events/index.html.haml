%h1 Listing events

%table
  %tr
    %th Title
    %th Description
    %th Location
    %th Start time
    %th End time
    %th Min volunteers
    %th Max volunteers
    %th Commitment type
    %th
    %th
    %th

  - @events.each do |event|
    %tr
      %td= event.title
      %td= event.description
      %td= event.location
      %td= event.start_time
      %td= event.end_time
      %td= event.min_volunteers
      %td= event.max_volunteers
      %td= event.commitment_type
      %td= link_to 'Show', event
      %td= link_to 'Edit', edit_event_path(event)
      %td= link_to 'Destroy', event, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Event', new_event_path
